import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // output: 'export',
  // basePath: '/jor_test1221',
  // trailingSlash: true, // Tambahkan ini
  // images: { unoptimized: true },
};

export default nextConfig;